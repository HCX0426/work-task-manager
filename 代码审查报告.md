# 「工作任务管理」前端代码审查报告

> 审查范围：`index.html` + `js/{store,entry,list,config,export,monthly,dashboard,help,app}.js`（共 10 个文件，约 3500 行，纯前端离线 SPA）。
> 审查方式：通读全部源码 + 核对 prior 段已落地的回归脚本（`_export`/`_cfg`/`_fix_ab`/`_dash`）。
> 结论：**未发现会在正常使用路径中导致崩溃或数据丢失的严重（致命）缺陷**；问题集中在边界处理、性能、安全卫生与可维护性，主要为「轻微」，少量「中等」。

---

## 一、严重（致命）

**无。** 代码防御性较好（`save` 对 `QuotaExceededError` 有显式拦截、关键保存点 `if(!save(...))return` 防误报、所有模板渲染走 `esc()` 转义、日期/状态校验齐全）。正常使用不会崩、不会丢数据。

---

## 二、中等（建议修复）

### M1. 甘特图 `Math.min(...dates)` / `Math.max(...dates)` 大数组栈溢出
- **位置**：`js/list.js:482-483`（`renderGantt` 的 `all` 分支）
- **触发场景**：选「全部时间」且任务很多时，`dates` 数组 = 每个任务的 开发/提出/结案日期，元素数可达数万~十万级。`Math.min(...dates)` 把数组展开成函数参数，超过 JS 引擎参数上限（约 6.5 万）抛 `RangeError: Maximum call stack size exceeded`。
- **预期影响**：甘特视图整段渲染崩溃（连带任务列表页卡死）。约 3 万+ 任务才可能触发，个人工具少见但属明确边界缺陷。
- **修复**：改为 `dates.reduce((a,b)=>Math.min(a,b), Infinity)` / `Math.max`。

### M2. 一次性迁移会覆盖自定义下拉（客户/厂区/提出部门）
- **位置**：`js/store.js:122-124`（v2 迁移 IIFE）
- **触发场景**：v1→v2 升级时（仅一次，受 `wb_cfg_v` 守卫，当前已置 `'2'` 不会再跑）。`dropdowns['客户']=DEFAULT_DROPDOWNS['客户'].slice()` 把用户自定义选项清空为默认值（如 `['所有']`），丢失 `其他/太白山/N客户` 等。
- **预期影响**：对升级用户是真实的数据丢失；对当前已升级用户无影响。代码语义是「覆盖」而非「合并」。
- **修复**：改为先合并用户已有选项再补默认（`[...new Set([...user, ...def])]`），与 `完成状态` 的处理方式对齐（同段 121 行已用 push 合并，逻辑不一致）。

### M3. AI Key 明文存储 + 可发往任意 URL（含非 https）
- **位置**：`config.js` `#cfgAiKey`（明文存 localStorage）、`store.js:14` `DEF_SETTINGS.aiKey`、`store.js:196-220` `aiChat`
- **触发场景**：用户在「配置中心 → AI 润色」填 Key。Key 以明文存于本机 `localStorage`（公用电脑可读）；`aiChat` 直连用户填的 `aiBaseUrl`，无 https/合法性校验，Key 经 `Authorization: Bearer` 发出。
- **预期影响**：属 BYOK 设计，但（1）明文落盘有泄漏风险；（2）若填 `http://` 地址，Key 明文走网络；（3）CORS/401 报错会回显。建议至少加：非 https 警告、对 `baseUrl` 做协议/格式校验、文档强调不在公用电脑填写（UI 已有提示，可再强化）。
- **修复优先级**：中（安全卫生，非紧急）。

### M4. 甘特图单任务/同日期间除零 → NaN 位置
- **位置**：`js/list.js:500`(`totalDays`)、`522`(`todayPos`)、`566-567`(`left`/`width`)
- **触发场景**：`all` 范围下仅有 1 条任务且 `start==end`（如只填 开发日期、未填 结案日期，`end` 回退为 `start`），则 `maxDate-minDate=0`，`(…)/(maxDate-minDate)` 得 `NaN`/`Infinity`。
- **预期影响**：今日线不绘制（被 `todayPos>=0&&<=100` 守卫拦掉），甘特条 `left:NaN%`/`width:NaN%` 失效（条形错位或不显示）。不崩溃，仅该视图视觉错乱。
- **修复**：分母统一用 `Math.max(1, maxDate-minDate)` 之类兜底。

---

## 三、轻微（可后续优化）

### m1. 导出预览与真实导出日期格式不一致
- **位置**：`export.js:125`（预览 `valOf` 永远 `fmtDateCN` → `yyyy/MM/dd`）vs `export.js:78` / `list.js:148`（真实导出按列 `dateFmt`，开发/测试/结案用 `MM/DD`）
- **触发**：预览页看到 `2026/08/27`，实际导出文件里是 `08/27`。
- **影响**：预览误导，用户以为格式一致，可能引起周报格式误会。
- **修复**：预览也按 `colDef.dateFmt` 走 `fmtDateMD`/`fmtDateCN`。

### m2. `entryDate` 未转义直接进 innerHTML（存储型 XSS 隐患）
- **位置**：`js/list.js:81`（卡片 pill）、`js/list.js:223`（回收站 pill）用 `${t.entryDate}` 未 `esc`
- **触发**：经「导入备份」写入畸形 `entryDate`（含 `<img onerror=...>` 等）后列表渲染。
- **影响**：本地单用户工具风险极低（需自己构造数据），但违反「用户输入必 `esc`」规范。
- **修复**：改为 `esc(t.entryDate)`（其余字段均已 `esc`）。

### m3. `loadSchema` 死分支 `type==='default'`
- **位置**：`js/store.js:77-84`
- **触发**：永不触发——`DEFAULT_SCHEMA` 与保存的 schema 类型均为 `auto/text/dropdown/date/textarea`，无 `type==='default'`；且 `wb_defaults` 从未被写入。
- **影响**：死代码，维护者易误判为有效迁移路径。
- **修复**：删除该分支或补上真实迁移来源。

### m4. 搜索框每次按键全量重渲染 + O(n·m) 过滤
- **位置**：`js/list.js:62`（`Object.values(t.values).some(...)`）、`244`（`oninput=renderList`）
- **触发**：任务多时每次 keystroke 遍历全部任务×全部字段并重建整段 DOM。
- **影响**：大数据量下输入卡顿。
- **修复**：输入防抖（debounce）+ 增量过滤。

### m5. `copyRowStyle` 逐格 `JSON.parse(JSON.stringify(...))`
- **位置**：`js/export.js:268`
- **触发**：开启「对齐上一行样式」且导出大表（逐单元格深拷贝 style）。
- **影响**：性能开销（大表明显）。
- **修复**：style 对象整体克隆一次后复用，或 `Object.assign({}, sc.style)`。

### m6. `parseDateAny` 的 `MM/DD` 分支按「当前年」解析
- **位置**：`js/store.js:148`
- **触发**：日期列/导入数据出现 `MM/DD` 文本且跨年（如 1 月录入历史 `12/30`）。
- **影响**：该日期被解析为「今年 12/30」，排序/筛选/逾期口径偏差。
- **修复**：结合上下文年份，或导入时显式标注无法定年。

### m7. 从 Excel 导入任务时 `entryDate` 可能为空
- **位置**：`js/list.js:428`（`values['提出日期']||values['开发日期']||''`）
- **触发**：导入的 Excel 行两个日期列都空。
- **影响**：该任务不出现在日历/本周已录/月报（均按 `entryDate` 聚合），仅在任务列表可查，易遗漏。
- **修复**：兜底为今天并在提示里标出「N 条缺录入日期」。

### m8. 部分 `save` 失败后内存与存储不一致
- **位置**：`js/list.js:113`（删除：`tasks.splice` 已改内存再 `save`）、`export.js`/`config.js` 部分 `save` 未判返回值
- **触发**：存储配额满时 `save` 返回 `false`，但内存已变，重载后本次操作丢失。
- **影响**：边界情况下「以为保存了其实没存」。主保存路径（`saveEntry`/`batchApply`）已严格 `if(!save)return`，但删除/合并路径未完全对齐。
- **修复**：统一「先 save 成功再改内存」，或失败回滚内存。

### m9. config 删除/新增列不即时持久化（与移动列不一致）
- **位置**：`js/config.js:126`(删除)、`214`(新增) 仅 `renderConfig()`，不 `save`；而 `moveCol`(`93-97`) 会 `save`
- **触发**：删除/加列后未点「保存列配置」就切走。
- **影响**：改动丢失。与「保存才生效」设计一致，但行为不一致易误解。
- **修复**：统一（要么都即时存，要么都在保存按钮处统一提示未保存）。

### m10. 入口 HTML 预渲染表单含硬编码日期 `2026-08-27`
- **位置**：`index.html:462-491` 静态 `#entryForm`
- **触发**：该静态表单始终被 `renderEntry(null)`（app.js:74）整体清空并重渲染，永不显示。
- **影响**：冗余 markup；将来改默认日期易漏改两处（SSR 一份 + `renderEntry` 一份）。
- **修复**：删冗余 SSR 表单，或统一由 `renderEntry` 生成。

### m11. `exceljs` 用 `defer`、模块脚本不用（加载顺序脆弱）
- **位置**：`index.html:20`（`defer`）vs `1141-1149`（经典脚本无 defer）
- **触发**：当前无模块顶层调用 `ExcelJS`，功能正常；但若将来任一模块顶层引用 `ExcelJS`，会因 defer 延后执行而 `ReferenceError`。
- **影响**：潜在脆弱性（当前无害）。
- **修复**：统一——要么全加 `defer`，要么 `exceljs` 去掉 `defer`（放 head 同步加载，保证早于底部模块）。

### m12. 统计聚合逻辑多处重复
- **位置**：`dashboard.js getDashboardData` / `list.js renderStats` / `entry.js todayTasks` / `monthly.js` 各自独立实现 `monthTasks`、`closed`、`overdue`、`byCust`、`bySt`
- **触发**：长期维护。
- **影响**：口径可能漂移（例如「逾期」定义改一处漏其他）。`dashboard.js` 注释自称「看板与 PDF 共用数据源」，但仍与 `entry.js.todayTasks` 重复。
- **修复**：抽一个公共 `aggregate(tasks)` 供各页复用。

---

## 四、已修复（历史，确认在版）

- **#35** 录入页持久节点（`#entryForm`/`#entryDate`）监听器随 `renderEntry` 累积 + 日历 `tail<0` 死分支 → 已修复（`entry.js:217-224` remove-before-add；`list.js:803` `const tail=Math.max(0,42-used)`）。
- **#D** 看板 PDF「本月任务明细」表头 `esc(c)`（c 为对象）→ `[object Object]` → 已改为 `esc(c.name)`（`dashboard.js:267`），回归 `_dash_reg.js` 10/10 通过（故意回退可复现 7 FAIL）。
- **#C1/#C2** 列模板/导入保留并按列 `dateFmt`（store.js:88-94 `loadSchema` 补 `dateFmt`；`applyColTemplate:260`；`importCfgFile:322-333`）——保证导出日期对齐真实周报模板（提出=ymd，其余=md）。

---

## 五、修复优先级建议

| 优先级 | 项 | 理由 |
|---|---|---|
| 高 | M1（栈溢出）、M4（除零 NaN） | 边界崩溃/视觉错乱，改起来小、收益大 |
| 中 | M2（迁移覆盖）、M3（Key 安全） | 一次性/安全，当前已无活跃触发但代码有损 |
| 低→中 | m1/m2/m6/m7 | 数据正确性与转义卫生，易踩坑 |
| 低 | m3/m4/m5/m8/m9/m10/m11/m12 | 性能/可维护性，按需排期 |

**总体评价**：工程完成度高、防御到位，无致命缺陷；按上表把 4 个「中等」边界/安全问题收口即可，轻微项可在空闲迭代。
