# 工作任务管理 — 修复后全面复查报告

复查时间：2026-09-01
复查对象：前一轮《代码审查报告》16 项修复（M1–M4 / m1–m12）的落地代码，以及受其影响的关联路径
复查方式：静态代码审计（逐处核对改动点及其调用链）+ 已回归脚本复核 + 针对性代码验证

## 结论

16 项修复本身**逻辑正确、已通过 115 条断言回归**。但复查发现 **2 处数据完整性风险（1 高 1 中）、1 处安全残留、1 处实现缺陷**，以及 10 项低优先级改进点。其中 2 处数据风险均由「多次 localStorage 写入非原子」这一共同根因引起，且恰好在**配额不足时**触发——而配额不足最可能发生的场景正是「恢复大备份」。

---

## 高（建议优先修复）

### P1 备份恢复：先改内存、不验返回值、无回滚、假成功提示
- **位置**：`js/list.js:286-293`（全量恢复 `#importAllFile`）；`js/list.js:329-333`（加密恢复，同构）
- **现状**：
  ```js
  tasks=d.tasks.filter(...); trash=...; schema=...; dropdowns=...;   // 先全量改内存
  save(LS_TASKS,tasks); save(LS_TRASH,trash); save(LS_SCHEMA,schema); save(LS_DROPDOWNS,dropdowns);  // 后逐个落盘，均不检查返回值
  ...
  toast('已恢复全量备份');   // 无论成败都报成功
  ```
- **影响**：任一 `save` 因 `QuotaExceededError` 失败，即产生「新列配置 + 新下拉 + 旧任务库」的混合态；界面仍提示「已恢复全量备份」，用户无法察觉，后续继续操作会把不一致状态固化。若首键（tasks，体积最大）失败而其余成功，则等于**恢复无效但配置被覆盖**。
- **触发条件**：localStorage 配额不足。恢复大备份正是写入量最大、最易触发的时机，因此触发概率不低。
- **修复建议**：
  1. 恢复前用 `getStorageInfo()` 估算体积，超限则先提示用户清理；
  2. 改为「全部 `save` 成功后再改内存」（沿用本次 m8 确立的口径）；
  3. 顺序落盘并在任一失败时**中止 + 回滚**（保存原值快照）或至少明确提示「恢复未完成，数据可能不一致，请勿关闭页面」；
  4. 去掉无条件成功 `toast`，改为按实际结果提示。

---

## 中

### P2 删除任务：两次保存顺序错误 + 无回滚，配额不足时任务双向丢失
- **位置**：`js/list.js:112`（单条删除）、`js/list.js:167`（批量删除）
- **现状**（本次 m8 修复后的代码）：
  ```js
  if(!save(LS_TASKS,nt)||!save(LS_TRASH,ntr)){ toast('保存失败，删除未生效'); return; }
  ```
  先落盘「移除任务」，再落盘「写入回收站」。
- **影响**：若第 1 次成功、第 2 次失败 → **磁盘 tasks 已删除该任务，但内存 `tasks` 未变、`trash` 也没有它**。此时界面显示任务仍在（提示「删除未生效」），但**一旦刷新页面，该任务既不在任务库也不在回收站 → 永久丢失**。这是本次修复暴露的残余原子性问题。
- **触发条件**：第二次写入触发配额错误（概率低，但后果为不可逆数据丢失）。
- **修复建议**（任选其一，推荐 1+2）：
  1. **颠倒顺序**：先 `save(LS_TRASH,ntr)` 再 `save(LS_TASKS,nt)`——失败时最坏结果是「回收站与任务库各有一份」，属**可回收的重复**，远优于丢失；
  2. **失败回滚**：第二次失败时 `save(LS_TASKS,tasks)` 还原原值；
  3. 提示文案改为「删除未生效，请检查存储空间后重试」。

### P3 AI Key 明文落盘并随备份外泄（M3 仅修了一半）
- **位置**：`js/store.js:14`（`DEF_SETTINGS.aiKey`）、`js/store.js:19` `loadSettings()`；`js/list.js:274` 全量备份 `settings:loadSettings()`
- **现状**：本轮 M3 修复只加了**服务地址协议校验**（防 Key 发往任意 URL），Key 本身仍以**明文 JSON** 存于 `localStorage.wb_exportcfg`，且全量备份（明文 `.json`）`settings` 字段**直接包含 aiKey**。
- **影响**：同机其他程序／浏览器扩展可读；全量备份文件一旦外发（如发给同事参考结构）即泄漏 Key。加密备份（`.wbe`）因整体加密，受密码保护，风险较低。配置备份（`config.js:308` 仅导出 `{schema,dropdowns}`）**不含 Key**——这一点是安全的。
- **修复建议**：
  1. 全量备份与加密备份的 `settings` 中剔除 `aiKey`（恢复后需用户重填，或单独提供「含 Key」的可选勾选项）；
  2. 配置中心 AI 区域明示「Key 以明文保存在本机浏览器，请勿在公用电脑保存」；
  3. 可选：改为会话内输入、不落盘（重启需重填），安全性最高但便利性下降。

### P4 配置中心「新增列」缺稳定 id 且不校验重名
- **位置**：`js/config.js:214`
  ```js
  $('#addCol').onclick=()=>{ schema.push({name:'新列',type:'text',def:''}); save(LS_SCHEMA,schema); renderConfig(); };
  ```
- **影响**：
  1. 新列**没有 `id`**，而 `computeRenames`（`store.js:297`）依赖 `c.id` 做改名识别——新增列在获得 id 前（直到下次 `#saveColCfg` 或页面重载）只能退化为按位置匹配，改名迁移可靠性下降；
  2. 连续点两次「新增列」会产生**两个同名 `新列`**，保存/重载后两者 id 均为 `col_新列`，`computeRenames` 的 `oldById/newById` 会互相覆盖 → 改名检测错乱；同名还会让 `matchCol`/`effMap`/`schema.find(c=>c.name===key)` 产生歧义。
  - 注：`#saveColCfg`（`config.js:293-295`）**已有**空名与重名校验，但该校验只覆盖「保存列配置」按钮，覆盖不到「新增列」按钮本身。
- **修复建议**：新增列时生成唯一 id（`id:'col_'+uid()`）与唯一默认名（`新列`、`新列2`…），从源头避免 id 冲突与重名。

---

## 低

| # | 位置 | 问题 | 影响 | 建议 |
|---|---|---|---|---|
| P5 | `index.html:20` | m11 去掉 `defer` 后，947KB `exceljs.min.js` 成为 `<head>` 内**阻塞脚本**，且位于 `<style>` 之前 | 首屏解析/渲染被整包阻塞，白屏时间变长 | 给**全部 10 个 script**（exceljs + 9 个模块）统一加 `defer`：defer 脚本按文档顺序执行、解析完成后 DOMContentLoaded 前触发，既保持 exceljs 先于模块执行，又不阻塞解析与首帧。这是比「去掉 defer」更优的解法 |
| P6 | `js/export.js:128-131` | m1 只修了「已加载模板」分支；未加载 Excel 模板时 `valOf=(task,c)=>task.values[c]||''`，日期按存储原样输出 | 同一预览表在「有/无模板」两种状态下日期格式不一致（`2026-08-06` vs `2026/08/06`），与导出结果的对应关系易混淆 | 无模板分支也复用同一按列 `dateFmt` 的格式化逻辑 |
| P7 | `js/config.js:126` | m9 删除列只改 `schema`，未联动清理 | ① `colMapping` 中指向已删列的值未清空，导出时静默写空；② `tasks[].values` 中该列数据成为孤儿，长期占用配额；③ 未调用 `renderEntry(null)`，录入页仍显示已删字段直到切换页签 | 删除时同步清理 `colMapping` 指向并触发 `renderEntry(null)`；孤儿历史数据可提供「清理孤儿字段」入口按需处理 |
| P8 | `js/config.js:301`、`js/config.js:126` | 配置保存仍是「先改内存、后 save」且**忽略 save 返回值**，与 m8 确立的「先保存成功再改内存」口径不一致 | save 失败时内存与磁盘分叉，且提示「列配置已保存」（假成功） | 统一为 `save` 成功后再赋值 schema |
| P9 | `js/store.js:329-341` | `aggregateTasks(ts, now)`：`monthTasks` 按入参 `now` 判年月，而 `overdue` 走 `isTaskOverdue`（内部用全局 `todayStr()`） | 传入非今天的 `now` 时两个口径参考日不同。当前生产调用均不传 `now`（默认今天），**暂无实际影响**，属潜伏问题 | 让 `isTaskOverdue` 接受参考日参数，或在 `aggregateTasks` 内统一参考日 |
| P10 | `js/store.js:332` vs `js/monthly.js:170-174` | 「本月任务」过滤存在两套等价实现（按 `now` 的 y/m vs 按 `'YYYY-MM'` 字符串） | 逻辑等价但两处维护，未来一方改动易漂移 | 统一为 `monthTasksOf(y,m)`，`aggregateTasks` 内部复用 |
| P11 | `js/export.js:264` | `const cols=Math.max(src.cellCount||0, excelHeaders.length, 20)` 硬编码下限 20 | 表头仅 5 列时仍复制 20 列样式；导出上千行时为 4 倍无谓开销 | 去掉 `20` 硬下限，改为 `Math.max(src.cellCount||0, excelHeaders.length)` |
| P12 | `js/store.js:96-121` | M2 对**已跑过旧版本**的用户无法自动补救：旧迁移是「覆盖」且同样写入 `wb_cfg_v='2'`，新代码因版本标记不再执行合并 | 曾运行旧版本的用户，其自定义下拉项已被覆盖丢失，无法通过升级找回 | 若有受影响用户，手动在配置中心补回；如需自动补救，将版本升至 3 并再执行一次 union（仅补默认项，不覆盖自定义） |
| P13 | `js/list.js` `renderGantt`/`renderList` | 视图为全量字符串重建（既有特征，非本次引入） | 千级以上任务时 `innerHTML` 全量重建较重；m4 的 200ms 防抖已缓解输入抖动，但未做增量/虚拟滚动 | 任务量上千时考虑分页或虚拟列表 |
| P14 | `work/_review_reg.js` | m8/m10/m11 仅静态确认，**无运行时断言** | 这 3 项无回归保护，后续改动可能静默破坏 | 补 3 条断言：① 模拟 `save` 失败断言删除不生效且内存不变；② 断言 `#entryForm` 初始为空、`renderEntry(null)` 后非空；③ 断言脚本顺序/无 `defer` 属性 |

---

## 已确认安全（本轮重点复核且未发现问题）

| 修复项 | 复核结论 |
|---|---|
| M1 甘特 reduce | `dates` 为空时已 `push(today)` 兜底（`list.js:488`），不会出现 reduce 空数组抛错；35000 任务回归通过 |
| M4 甘特除零/死循环 | 周表头循环 `cursor` 每轮固定 +7 天推进，无死循环；`span=0` 时 `totalDays=1`、`todayPos=0`、`denom=1`，无 NaN |
| M3 协议校验 | 校验位于 `fetch` 之前（`store.js:196`），非法地址不会发出请求，Key 不外泄 |
| m5 `copyRowStyle` 浅拷贝 | 逐处核对 `export.js` 全部样式写入点（7/8/291/296）：均为「属性**重新赋值**」而非原地修改嵌套对象，故源行样式不会被目标行污染，浅拷贝在当前用法下安全。唯一遗留耦合：`cell.font=EXCEL_FONT` 使所有单元格共享同一常量对象，未来若出现 `cell.font.bold=true` 的原地修改会污染全局常量（当前无此类代码） |
| m2 `esc` | 覆盖 `& " < >`，录入日期等场景转义正确 |
| m6 `parseDateAny` | 非法日期（如非闰年 `02/29`）正确返回 `null`，不会误判 |
| m7 导入兜底 | 缺「提出日期」且缺「开发日期」的行兜底为今天并计数提示，端到端回归通过 |
| m10 空表单 | `js/app.js:74` 确认在初始化调用 `renderEntry(null)`，HTML 空表单不会导致空白页 |
| m12 口径一致 | 列表/看板/月报三处统计已由回归断言对齐（`monthCount`/`rate`/`overdueCount`/`closedAll` 与月报条数一致） |

---

## 建议的修复顺序

1. **P1 + P2**（数据完整性，同一根因：多次写入非原子）→ 一并处理，统一为「先落盘成功再改内存 + 失败可回滚/可恢复」；
2. **P3**（Key 明文）→ 备份剔除 `aiKey` + UI 明示风险；
3. **P4**（新增列 id/重名）→ 一行改动，收益明确；
4. **P5/P6/P7/P8**（体验与一致性）→ 顺手清理；
5. **P9–P14**（潜伏与优化）→ 按需排期。
